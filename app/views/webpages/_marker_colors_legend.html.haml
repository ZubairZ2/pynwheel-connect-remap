.legend
  - legend_items.each do |item|
    - style = item[:color].present? ? "background: #{item[:color]}" : ""
    .legend-item
      .color-box{ style: style }
      %span.label= item[:label].upcase
