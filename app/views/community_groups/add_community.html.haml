- content_for :title, "Add Community - Pynwheel"
%section.is-community-page
  .content
    .box.box-info.add-plan.mt-20
      .box-header
        %h3.box-title
          Add
          %span Community

      = form_for @community_group,:url=>save_community_community_groups_path(@community_group),:html=>{:class=>"form-horizontal"} do |f|
        .box-body
          .row
            .col-lg-9.col-lg-offset-1
              .form-group
                %label.col-sm-12.col-lg-3.col-md-3.col-xs-12.control-label
                  Communities
                  %span Select communities
                .col-sm-12.col-lg-9.col-md-9.col-xs-12


                  -if current_user.is_super_admin?
                    = f.select :communities, options_for_select(@company.communities.collect{|c| [c.name,c.id]},f.object.communities.map(&:id)),{},{class: "form-control group_list",:multiple=>true}

          .row
            .col-lg-9.col-lg-offset-1
              .form-group
                %label.col-sm-12.col-lg-3.col-md-3.col-xs-12.control-label
                  Application background color opacity
                .col-sm-12.col-lg-9.col-md-9.col-xs-12
                  = select_tag 'master', options_for_select([]),{:class => "form-control master_community_class"}




        / /.box-body
        .box-footer
          = link_to "CANCEL" , company_community_group_path((Company.find @community_group.company_id).id,@community_group.id), :class=>"btn btn-default pull-right"
          = f.submit "ADD COMMUNITY", :class=>"btn btn-success pull-right mr-10"

:javascript
  $('#community_group_communities').multiSelect();
  try {
    $(".group_list").change(function(e){
    var selected=[];
   $('.group_list :selected').each(function(){
       selected.push($(this).text());
   });
   added = [];
   var x = document.getElementById("master");
   // for (j=0; j < x.options.length;j++)
   // {
   //    added.push(x.options[j].value);
   // }
   $('#master').empty()


   for (i = 0; i < selected.length; i++) {
    var option = document.createElement("option");
    option.text = selected[i];
      // if (!(added.includes(selected[i])))
      // {
      x.add(option,selected[i]);
      // }
    }

  });
   var selected=[];
   $('.group_list :selected').each(function(){
       selected.push($(this).text());
   });
   var x = document.getElementById("master");
   for (i = 0; i < selected.length; i++) {
    var option = document.createElement("option");
    option.text = selected[i];
    x.add(option,selected[i]);
    }

    x.value = "#{@selected.name}"
  }
  catch (e) {  }
