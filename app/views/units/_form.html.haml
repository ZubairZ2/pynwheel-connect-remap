.form-group
  %label.col-sm-12.col-lg-3.col-md-3.col-xs-12.control-label
    Manual Override
    %i.unit_manualoverride.fa.fa-question-circle
      %span#unit_manualoverride_text
        When Manual Override is set to Yes, then the items that have been changed will not update automatically from the property's property management software data feed (if the property is using a third party data feed).

  .col-sm-12.col-lg-2.col-md-2.col-xs-12
    = f.radio_button :manual_override, true, class: "settings-field"
    = f.label :manual_override, "Yes", :value => "true"
  .col-sm-12.col-lg-2.col-md-2.col-xs-12
    = f.radio_button :manual_override, false, class: "settings-field"
    = f.label :manual_override, "No", :value => "false"
.form-group
  %label.col-sm-12.col-lg-3.col-md-3.col-xs-12.control-label

  .col-sm-12.col-lg-9.col-md-9.col-xs-12
    .row
      .col-sm-6.col-lg-4.col-md-3.col-xs-12
        = f.select :floorplan_id, @community.floorplans.collect{|f| [f.name,f.provider_floorplan_id]},{},{ :class=>"form-control"}
        %small Floor plan (Manual Override Required)
      .col-sm-6.col-lg-4.col-md-3.col-xs-12
        = f.select :availability, [['No', 'Occupied'], ['Yes', 'Unoccupied']],{},{ :class=>"form-control"}
        %small Availability (Manual Override Required)
      .col-sm-6.col-lg-4.col-md-3.col-xs-12
        = f.text_field :provider_unit_id, :class=>"form-control validate[required]", :placeholder=>"Provider Unit ID"
        %small Provider Unit ID  
.form-group
  %label.col-sm-12.col-lg-3.col-md-3.col-xs-12.control-label

  .col-sm-12.col-lg-9.col-md-9.col-xs-12
    .row
      .col-sm-6.col-lg-4.col-md-4.col-xs-12

        = f.text_field :effective_rent, :class=>"form-control validate[required,custom[onlyDecimal]]", :placeholder=>"Rent",:maxlength=>"9", :value => f.object.effective_rent.to_i
        %small Price (Manual Override Required)
      .col-sm-6.col-lg-4.col-md-4.col-xs-12
        = f.text_field :marketing_name, :class=>"form-control validate[required]", :placeholder=>"Marketing Name"
        %small Marketing Name (Manual Override Required)
      .col-sm-6.col-lg-4.col-md-4.col-xs-12
        = f.text_field :available_date, :class=>"form-control datepicker", :placeholder=>"Available Date",:maxlength=>"15",:autocomplete=>"off",:readonly=>true
        %small Available Date (Manual Override Required)
.form-group
  %label.col-sm-12.col-lg-3.col-md-3.col-xs-12.control-label

  .col-sm-12.col-lg-9.col-md-9.col-xs-12
    .row
      .col-sm-6.col-lg-4.col-md-4.col-xs-12
        = f.text_field :building, :class=>"form-control", :placeholder=>"Building",:maxlength=>"20"
        %small Building (Manual Override Required)
      - unless @community.is_sitemap
        - if f.object.floorplate.present?
          .col-sm-6.col-lg-4.col-md-4.col-xs-12
            = f.select :floor, f.object.floorplate.floors,{}, :class=>"form-control validate[custom[floor]]"
            %small Floor (Manual Override Required)
        - else
          .col-sm-6.col-lg-4.col-md-4.col-xs-12
            = f.text_field :floor, :class=>"form-control validate[custom[floor]]", :placeholder=>"Floor"
            %small Floor (Manual Override Required)
      .col-sm-6.col-lg-4.col-md-4.col-xs-12
        = f.text_field :square_feet, :class=>"form-control ", :placeholder=>"Unit space", :value => f.object.square_feet.to_i
        %small Square footage (Manual Override Required)

      .col-sm-6.col-lg-4.col-md-4.col-xs-12
        = f.text_field :unit_status, :class=>"form-control ", :placeholder=>"Unit Status", :value => f.object.unit_status
        %small Unit Status

= f.hidden_field :provider

-#.form-group
-#  %label.col-sm-12.col-lg-3.col-md-3.col-xs-12.control-label
-#    Display Virtual Tour Button Label
-#  .col-sm-12.col-lg-9.col-md-9.col-xs-12
-#    .TriSea-technologies-Switch
-#      %span.pr-15 No
-#      = f.check_box :display_virtual_tour_button_label,:class=>"form-control"
-#      %label.label-primary{:for => "unit_display_virtual_tour_button_label"}
-#      %span.pl-15 Yes

.form-group#unit_virtual_tour_button_label_field
  %label.col-sm-12.col-lg-3.col-md-3.col-xs-12.control-label
    Virtual Tour Button Label

    %span Specify virtual tour button label here
  .col-sm-12.col-lg-9.col-md-9.col-xs-12
    = f.text_field :virtual_tour_button_label, :class=>"form-control", :placeholder=>"Virtual Tour Button Label",:maxlength=>"15"
  
.form-group
  %label.col-sm-12.col-lg-3.col-md-3.col-xs-12.control-label
    Virtual Tour URL
    %span Specify URL here
  .col-sm-12.col-lg-9.col-md-9.col-xs-12
    = f.text_field :virtual_tour_url, :class=>"form-control", :placeholder=>"Virtual Tour URL"

- if @community.enable_locks
  - locks_present_hash = {}
  - locks_present_hash["Dwelo"] = @community.multiple_locks_provider.include?("Dwelo") && @community.dwelo.present? && @all_locks[:dwelo_locks].any?
  - locks_present_hash["EdgeState"] = @community.multiple_locks_provider.include?("EdgeState") && @community.edge_state.present? && @all_locks[:edgestate_locks].any?
  - locks_present_hash["Latch"] = @community.multiple_locks_provider.include?("Latch") &&  @community.latch.present? && @all_locks[:latch_locks].any?
  - locks_present_hash["Zerv"] = @community.multiple_locks_provider.include?("Zerv") &&  @community.zerv.present? && @all_locks[:zerv_locks].any?
  - locks_present_hash["Igloohome"] = @community.multiple_locks_provider.include?("Igloohome") &&  @community.igloohome.present? && @all_locks[:igloohome_locks].any?

  // load options and input values start
  - if locks_present_hash["Dwelo"]
    = hidden_field_tag "dwelo_lock_input_value", (@door.present?) ? (@door.dwelo_lock.present? ? @door.dwelo_lock.name + " (" + @door.dwelo_lock.remote_lock_type + ")"  : '') : ( @unit.dwelo_locks.present? ? @unit.dwelo_locks.first.name + " (" + @unit.dwelo_locks.first.remote_lock_type + ")" : '' )
    .dwelo_options.hide_it
      - @all_locks[:dwelo_locks].each do |lock|
        %option{value: "#{lock[:name]}", 'data-value'=> "#{lock[:id]}"}
  - if locks_present_hash["EdgeState"]
    = hidden_field_tag "edgestate_lock_input_value", (@door.present?) ? (@door.edgestate_lock.present? ? @door.edgestate_lock.name + " (" + @door.edgestate_lock.remote_lock_type + ")"  : '') : ( @unit.edgestate_locks.present? ? @unit.edgestate_locks.first.name + " (" + @unit.edgestate_locks.first.remote_lock_type + ")" : '' )
    .edgestate_options.hide_it
      - @all_locks[:edgestate_locks].each do |lock|
        %option{value: "#{lock[:name]}", 'data-value'=> "#{lock[:id]}"}  
  - if locks_present_hash["Latch"]
    = hidden_field_tag "latch_lock_input_value", (@door.present?) ? (@door.latch_lock.present? ? @door.latch_lock.lock_name : '') : ( @unit.latch_locks.present? ? @unit.latch_locks.first.lock_name : '' )
    .latch_options.hide_it
      - @all_locks[:latch_locks].each do |lock|
        %option{value: "#{lock[:name]}", 'data-value'=> "#{lock[:id]}"}
  - if locks_present_hash["Zerv"]
    = hidden_field_tag "zerv_lock_input_value", (@door.present?) ? (@door.zerv_lock.present? ? @door.zerv_lock.sub_location_name + " (" + @door.zerv_lock.sub_location_friendly_name + ")" : '') : ( @unit.zerv_locks.present? ?  @unit.zerv_locks.first.sub_location_name + " (" + @unit.zerv_locks.first.sub_location_friendly_name + ")" : '' )
    .zerv_options.hide_it
      - @all_locks[:zerv_locks].each do |lock|
        %option{value: "#{lock[:name]}", 'data-value'=> "#{lock[:id]}"}
  - if locks_present_hash["Igloohome"]
    = hidden_field_tag "igloohome_lock_input_value", (@door.present?) ? (@door.igloohome_lock.present? ? @door.igloohome_lock.device_name : '') : ( @unit.igloohome_locks.present? ? @unit.igloohome_locks.first.device_name : '' )
    .igloohome_options.hide_it
      - @all_locks[:igloohome_locks].each do |lock|
        %option{value: "#{lock[:name]}", 'data-value'=> "#{lock[:id]}"}  

  .form-group
    %label.col-sm-12.col-lg-3.col-md-3.col-xs-12.control-label
      Select Lock Type
      %span Select the access provider for this stop
    .col-sm-12.col-lg-9.col-md-9.col-xs-12
      = f.select :lock_provider , options_for_select(@community.lock_options(locks_present_hash), (@door.present?) ? @door.lock_provider : f.object.lock_provider),{},{:class=>"form-control lock_provider_input"}

  .form-group.hide_it#manual_access_code
    %label.col-sm-12.col-lg-3.col-md-3.col-xs-12.control-label
      Access Code
    .col-sm-12.col-lg-9.col-md-9.col-xs-12
      - access_code_value = f.object.door.present? ? f.object.door.access_code : f.object.access_code
      = f.text_field :access_code, :oninput => "this.value = this.value.replace(/[^0-9#*]/g, '')", :class=>"form-control", :placeholder=>"Access Code",:maxLength => "6", :value => access_code_value

  .form-group#lock_access_code.hide_it
    %label.col-sm-12.col-lg-3.col-md-3.col-xs-12.control-label
    
    .col-sm-12.col-lg-9.col-md-9.col-xs-12
      = text_field_tag :lock_input, '', :placeholder => "Search by lock name", :list => "locks_list", :autocomplete=> "off", :type => "text", :class => "form-control lock_provider_input"
      %datalist#locks_list
    = hidden_field_tag "lock_id", ""

- if @community.touchscreen_app
  .form-group
    %label.col-sm-12.col-lg-3.col-md-3.col-xs-12.control-label
      Details

    .col-sm-12.col-lg-9.col-md-9.col-xs-12
      = f.text_area :description,:class=>"form-control mb-20 validate[required] unit-description-wysihtml5",:placeholder=>"Enter Description"
      <!-- %span.unit_details_text{:style => "float: right; margin: 10px 15px 0px 0px;"} Character limit: #{ENV["DESCRIPTION_LIMIT"].to_i} -->

- if @community.self_tour
  .form-group
    %label.col-sm-12.col-lg-3.col-md-3.col-xs-12.control-label
      Directional Text
      %i.propertyDetailName.fa.fa-question-circle{ "data-toggle" => "propertyDetailAddressPopover","data-content" => " "}
        %span#propertyDetailNameText
          This description will appear above the image. Only the first #{ENV["DESCRIPTION_LIMIT"].to_i} characters will appear on the page with the image; the rest of the description can be accessed by pulling up a separate page in the app.
    .col-sm-12.col-lg-9.col-md-9.col-xs-12
      = f.text_area :stop_description,:class=>"form-control mb-20 wysihtml5",:placeholder=>"Enter Directional Description", :rows => "8"
      <!-- %span.unit_directional_text{:style => "float: right; margin: 10px 15px 0px 0px;"} Character limit: #{ENV["DESCRIPTION_LIMIT"].to_i} -->
-#.form-group
-#  %label.col-sm-12.col-lg-3.col-md-3.col-xs-12.control-label
-#    Available
-#
-#  .col-sm-12.col-lg-1.col-md-1.col-xs-12
-#    = f.radio_button :available, true, class: "settings-field"
-#    = f.label :manually_updated, "Yes", :value => "true"
-#  .col-sm-12.col-lg-2.col-md-2.col-xs-12
-#    = f.radio_button :available, false, class: "settings-field"
-#    = f.label :available, "No", :value => "false"

.form-group
  %label.col-sm-12.col-lg-3.col-md-3.col-xs-12.control-label
    Sold
    %i.unit_sold.fa.fa-question-circle
      %span#unit_sold_text
        This option is for For Sale properties only. When a Sold is set to Yes, then the details on the unit pop-up will show that the unit is sold.

  .col-sm-12.col-lg-2.col-md-2.col-xs-12
    = f.radio_button :sold, true, class: "settings-field"
    = f.label :sold, "Yes", :value => "true"
  .col-sm-12.col-lg-2.col-md-2.col-xs-12
    = f.radio_button :sold, false, class: "settings-field"
    = f.label :sold, "No", :value => "false"          

.form-group
  %label.col-sm-12.col-lg-3.col-md-3.col-xs-12.control-label
    Model Unit
  .col-sm-12.col-lg-9.col-md-9.col-xs-12
    .row
      .col-sm-6.col-lg-6.col-md-4.col-xs-12
        .checkbox
          = f.check_box :modal_unit
          %label Model unit

.form-group
  %label.col-sm-12.col-lg-3.col-md-3.col-xs-12.control-label{style: "margin-top: 50px;"}
    Primary Floor Plan Image
    %span
      THIS WILL REPLACE THE STANDARD FLOOR PLAN IMAGE FOR THIS UNIT.
      %br/
      Images will appear at a size of 325 x 300 px. Please note that images that are too small may appear pixelated.

  .col-sm-12.col-lg-9.col-md-9.col-xs-12
    -#.select-img.mb-20
    -#  .select-img-img
    .col-lg-5.col-md-5.col-sm-6.col-xs-6
      .slide-image
        .image-box.video-box
          .images.height-200
            - rn =  rand(9999999999999999999999999)
            %a.preview-image{:href=>f.object.image.present? ? (f.object.image.url + "?timestamp=#{rn}") : asset_path("default.jpeg"),"data-toggle"=>"lightbox","data-title"=>"Unit Primary Image"}
              = image_tag f.object.image.present? ? (f.object.image.url + "?timestamp=#{rn}") : "default.jpeg",:height=>"",:width=>"170",:id=>"preview-image", :style => "max-height: 190px; max-width: 270px;"
          = image_tag "exclamation-mark.png", class: "waring_exal #{f.object.image.present? ? ((f.object.image.width < 300 && f.object.image.height < 250) ? "" : "hidden") : "hidden"}", style: "width: 25px", "data-content" => "Small Image. May appear pixelated.", "data-toggle" => "popover", "data-trigger" => "hover", :href => "#"
        - if f.object.image.present?
          .icons
            %a.text-danger{"data-name"=>"Image","data-href" => "/communities/#{@community.id}/units/#{@unit.id}/remove_pri_scnd_image?image=primary", "data-target" => "#confirm-delete", "data-toggle" => "modal", :href => "javascrip::;"}
              %i.fa.fa-close
        .row
          .col-lg-6
            %span.btn.btn-default.black-button.btn-sm.btn-file
              Upload a new image
              = f.file_field :image
          .col-lg-6
            - if  f.object.image.present? && f.object.image.content_type != 'image/svg+xml'
              .icon
                = link_to show_unit_image_in_modal_community_unit_path(), remote: true do
                  %span.btn.btn-default.black-button.btn-sm.btn-file{"data-target" => "#edit-name", "data-toggle" => "modal"}
                    Crop

.form-group
  %label.col-sm-12.col-lg-3.col-md-3.col-xs-12.control-label{style: "margin-top: 50px;"}
    Secondary Floor Plan Image
    %span
      Upload secondary floor plan image of this Unit.
      %br/
      Images will appear at a size of 325 x 300 px. Please note that images that are too small may appear pixelated.

  .col-sm-12.col-lg-9.col-md-9.col-xs-12
    -#.select-img.mb-20
    -#  .select-img-img
    .col-lg-5.col-md-5.col-sm-6.col-xs-6
      .slide-image
        .image-box.video-box
          .images.height-200
            - rn =  rand(9999999999999999999999999)
            %a.preview-image{:href=>f.object.secondary_image.present? ? (f.object.secondary_image.url  + "?timestamp=#{rn}") : asset_path("default.jpeg"),"data-toggle"=>"lightbox","data-title"=>"Unit Secondary Image"}
              = image_tag f.object.secondary_image.present? ? (f.object.secondary_image.url  + "?timestamp=#{rn}") : "default.jpeg",:height=>"",:width=>"170",:id=>"preview-secondary-image", :style => "max-height: 190px; max-width: 270px;"
          = image_tag "exclamation-mark.png", class: "waring_exal #{f.object.secondary_image.present? ? ((f.object.secondary_image.width < 300 && f.object.secondary_image.height < 250) ? "" : "hidden") : "hidden"}", style: "width: 25px", "data-content" => "Small Image. May appear pixelated.", "data-toggle" => "popover", "data-trigger" => "hover", :href => "#"
        - if f.object.secondary_image.present?
          .icons
            %a.text-danger{"data-name"=>"Image","data-href" => "/communities/#{@community.id}/units/#{@unit.id}/remove_pri_scnd_image?image=secondary", "data-target" => "#confirm-delete", "data-toggle" => "modal", :href => "javascrip::;"}
              %i.fa.fa-close
        .row
          .col-lg-6
            %span.btn.btn-default.black-button.btn-sm.btn-file
              Upload a new image
              = f.file_field :secondary_image
          .col-lg-6
            - if  f.object.secondary_image.present? && f.object.secondary_image.content_type != 'image/svg+xml'
              .icon
                = link_to show_unit_secondary_image_in_modal_community_unit_path(), remote: true do
                  %span.btn.btn-default.black-button.btn-sm.btn-file{"data-target" => "#edit-name", "data-toggle" => "modal"}
                    crop
:javascript
  var characterLimit = parseInt("#{ENV["DESCRIPTION_LIMIT"]}")
  var community_id = #{params[:community_id]}
  var unit_id = #{@unit.id}
  var door = '#{(@community.auto_wayfinding and @door.present?) ? "present" : "undefined"}'
  
  var locks_input,locks_list = "#locks_list";
  $(document).ready(function(){
    // run it on ready 
    update_locks_select_and_lock_code_field()
    // provide locks on change 
    $('#unit_lock_provider').change(function(){
      update_locks_select_and_lock_code_field();
    });

    function update_locks_select_and_lock_code_field(){
      // if any lock selected 
      if ($('#unit_lock_provider').val().length > 0 && $('#unit_lock_provider').val() != "Manual" ){
        $('#manual_access_code').addClass('hide_it')
        // initialize elements 
        lock_provider_type = $('#unit_lock_provider').val().toLowerCase()
        lock_input_value = "#" + lock_provider_type + "_lock_input_value"
        options = '.' + lock_provider_type + "_options"
        locks_input = "#lock_input"
        // assign values
        if (lock_provider_type == "zerv")
          $('#lock_access_code').find('label').html("Lock <span>Assign the lock to this unit</span>")
        else
          $('#lock_access_code').find('label').html(capitalize(lock_provider_type) + " Lock <span>Assign the lock to this unit</span>")

        $('#lock_input').val( $(lock_input_value).val() )
        $('datalist#locks_list').html($(options).html())
        $('#lock_access_code').removeClass('hide_it') 
      }
      else if ($('#unit_lock_provider').val().length > 0){
        $('#lock_access_code').addClass('hide_it')
        $('#manual_access_code').removeClass('hide_it')
        locks_input = undefined
      }  
      else
      {
        $('#lock_access_code').addClass('hide_it')
        $('#manual_access_code').addClass('hide_it')
      }
    }
    setTimeout(function(){ $('.lock_provider_input').change(); }, 500);
    $('.lock_provider_input').change(function(){
      if (locks_input){
        var lock = $(locks_input)[0];
        if(lock.value == "")
            lock.setCustomValidity('')

        if(lock.checkValidity() == true){
          lock_id = $(locks_list + ' [value="' + lock.value + '"]').data('value')
          $('#lock_id').val(lock_id)
        }
        //else{ lock.reportValidity(); }
      }
    });

  });

  $(".datepicker").datepicker({dateFormat: 'yy-mm-dd' });
  $(document).ready(function(){
      $('.wysihtml5').each(function(i, elem) {
        $(elem).wysihtml5({'toolbar': {'image': false,'link' : true},
        events: {
                change: function(){
                    var wysihtml5Editor = $('#unit_stop_description').data("wysihtml5").editor;
                    var t = wysihtml5Editor.getValue();
                    t1 = t.substr(0, characterLimit)
                    t2 = t.substr(characterLimit, t.length)
                    <!-- t2 = t2.fontcolor("red"); -->
                    wysihtml5Editor.setValue(t1 + t2);
                },
                load: function(){
                    $('.wysihtml5-sandbox').contents().find('body').on("keydown",function(event) {
                    if (wysihtml5Editor.getValue() != "")
                    {
                        var text_split = $('.unit_directional_text').text().split(" ")
                        var total_length = wysihtml5Editor.getValue().replace(/<(?:.|\n)*?>/gm, '').replace(/(\r\n|\n|\r)/gm,"").replace(/\&nbsp;/g, '').length
                        $('.unit_directional_text').text( text_split[0] + " " + text_split[1] + " " + (characterLimit - total_length)).toString()
                        }
                    });
                    var wysihtml5Editor = $('#unit_stop_description').data("wysihtml5").editor;
                    var t = wysihtml5Editor.getValue();
                    t1 = t.substr(0, characterLimit)
                    t2 = t.substr(characterLimit, t.length)
                    <!-- t2 = t2.fontcolor("red"); -->
                    wysihtml5Editor.setValue(t1 + t2);
                    var text_split = $('.unit_directional_text').text().split(" ")
                    var total_length = wysihtml5Editor.getValue().replace(/<(?:.|\n)*?>/gm, '').replace(/(\r\n|\n|\r)/gm,"").replace(/\&nbsp;/g, '').length
                    $('.unit_directional_text').text( text_split[0] + " " + text_split[1] + " " + (characterLimit - total_length)).toString()

                }
            }
        });
      });

      $('.unit-description-wysihtml5').each(function(i, elem) {
        $(elem).wysihtml5({'toolbar': {'image': false,'link' : true},
        events: {
                change: function(){
                    var wysihtml5Editor = $('#unit_description').data("wysihtml5").editor;
                    var t = wysihtml5Editor.getValue();
                    t1 = t.substr(0, characterLimit)
                    t2 = t.substr(characterLimit, t.length)
                    <!-- t2 = t2.fontcolor("red"); -->
                    wysihtml5Editor.setValue(t1 + t2);
                },
                load: function(){
                    $('.wysihtml5-sandbox').contents().find('body').on("keydown",function(event) {
                    if (wysihtml5Editor.getValue() != "")
                    {
                        var text_split = $('.unit_details_text').text().split(" ")
                        var total_length = wysihtml5Editor.getValue().replace(/<(?:.|\n)*?>/gm, '').replace(/(\r\n|\n|\r)/gm,"").replace(/\&nbsp;/g, '').length
                        $('.unit_details_text').text( text_split[0] + " " + text_split[1] + " " + (characterLimit - total_length)).toString()
                        }
                    });
                    var wysihtml5Editor = $('#unit_description').data("wysihtml5").editor;
                    var t = wysihtml5Editor.getValue();
                    t1 = t.substr(0, characterLimit)
                    t2 = t.substr(characterLimit, t.length)
                    <!-- t2 = t2.fontcolor("red"); -->
                    wysihtml5Editor.setValue(t1 + t2);
                    var text_split = $('.unit_details_text').text().split(" ")
                    var total_length = wysihtml5Editor.getValue().replace(/<(?:.|\n)*?>/gm, '').replace(/(\r\n|\n|\r)/gm,"").replace(/\&nbsp;/g, '').length
                    $('.unit_details_text').text( text_split[0] + " " + text_split[1] + " " + (characterLimit - total_length)).toString()

                }
            }
        });

      });


  })
  if (localStorage.getItem("floorplanCropped") == "1")
  {
    localStorage.setItem("floorplanCropped", "0");
    location.reload(true);
  }


