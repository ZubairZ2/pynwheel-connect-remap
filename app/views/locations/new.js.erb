$('#show_modal').html("<%= escape_javascript render 'add_location' %>");
$("#addLocation").modal('show');
$("#new_location").validationEngine({binded: false});


  $('#location_image').change(function(){
    input = document.getElementById('location_image');
      if (input.files[0].type == "image/png" || input.files[0].type == "image/jpeg" || input.files[0].type == "image/jpg") {

      }
      else{$('#image-upload-warning').modal('show');
          $(input).val('');}
    if (input.files[0].size > 10000000)
    {
      $('#image-size-warning').modal('show');
      $(input).val('');
    }
});
