<%= form_for(elevator) do |f| %>
  <% if elevator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(elevator.errors.count, "error") %> prohibited this elevator from being saved:</h2>

      <ul>
      <% elevator.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :x_plot %>
    <%= f.number_field :x_plot %>
  </div>

  <div class="field">
    <%= f.label :y_plot %>
    <%= f.number_field :y_plot %>
  </div>

  <div class="field">
    <%= f.label :directional_text %>
    <%= f.text_field :directional_text %>
  </div>

  <div class="field">
    <%= f.label :floorplate_id %>
    <%= f.text_field :floorplate_id %>
  </div>

  <div class="field">
    <%= f.label :community_id %>
    <%= f.text_field :community_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
